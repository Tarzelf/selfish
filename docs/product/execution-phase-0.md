# Execution plan — Phase 0

## Goal
Ship a tasteful web listening MVP that can run in **demo mode** (no keys) and **live mode** (Grok + ElevenLabs) for closed women testers.

## Build order
1. Expo app scaffold (web)
2. Shared types + script rails + safety policy
3. UI: age gate → mood ritual → generating → player → feedback
4. API route stubs for generate-session
5. Demo script library (structure only / literary fade — no graphic dumps in repo samples)
6. Env template for secrets

## Out of scope this PR
- App Store binary
- Stripe live billing
- Production Supabase project (schema documented; wire when keys exist)
