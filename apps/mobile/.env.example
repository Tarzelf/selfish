# Selfish — environment template

Copy to `apps/mobile/.env` for live generation (optional; app runs in demo mode without these).

```
EXPO_PUBLIC_DEMO_MODE=true
XAI_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
SUPABASE_URL=
SUPABASE_ANON_KEY=
```

Never commit real secrets.
